["SEED"]
prototype = "SEED"
prototype_set = "SEED x"
short = "get / set the random number generator seed for all `SEED` ops"

["RAND.SEED"]
prototype = "RAND.SEED"
prototype_set = "RAND.SEED x"
aliases = ["RAND.SD", "R.SD"]
short = "get / set the random number generator seed for `R`, `RRAND`, and `RAND` ops"

["TOSS.SEED"]
prototype = "TOSS.SEED"
prototype_set = "TOSS.SEED x"
aliases = ["TOSS.SD"]
short = "get / set the random number generator seed for the `TOSS` op"

["PROB.SEED"]
prototype = "PROB.SEED"
prototype_set = "PROB.SEED x"
aliases = ["PROB.SD"]
short = "get / set the random number generator seed for the `PROB` mod"

["DRUNK.SEED"]
prototype = "DRUNK.SEED"
prototype_set = "DRUNK.SEED x"
aliases = ["DRUNK.SD"]
short = "get / set the random number generator seed for the `DRUNK` op"

["P.SEED"]
prototype = "P.SEED"
prototype_set = "P.SEED x"
aliases = ["P.SD"]
short = "get / set the random number generator seed for the `P.RND` and `PN.RND` ops"

["MRBL.SD"]
prototype = "MRBL.SD"
prototype_set = "MRBL.SD x"
short = "get / set the random number generator seed for the `MRBL.V`, `MRBL.D`, and `MRBL.T` ops"

["CA.SEED"]
prototype = "CA.SEED v"
short = "Seeds the cellular-automaton row from `v`. `v = 0` plants a single centre cell; otherwise `v`'s 16 bits are written centred in the row"
description = """
`CA.SEED` (re)seeds the `CA` row. `CA.SEED 0` plants the canonical single centre cell (guaranteed alive). A non-zero `v` writes its 16 bits centred in the 32-cell row, giving a deterministic custom starting pattern. Combine with `RND`/`RRAND` for a random seed, e.g. `CA.SEED RND 32767`. Reseeding makes a run reproducible from a known starting point.
"""
